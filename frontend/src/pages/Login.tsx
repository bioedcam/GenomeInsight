import PlaceholderPage from "@/components/PlaceholderPage"

export default function Login() {
  return (
    <PlaceholderPage
      moduleName="Login"
      phase={4}
      description="Optional PIN/password authentication with bcrypt hashing and 4-hour session timeout."
    />
  )
}
