import PlaceholderPage from "@/components/PlaceholderPage"

export default function ReportBuilder() {
  return (
    <PlaceholderPage
      moduleName="Reports"
      phase={4}
      description="Module-level report generation — select modules, preview, and download as PDF with evidence cards and charts."
    />
  )
}
